import React, {useState} from 'react';
import {Text, View, Center} from 'native-base';

const AccountScreen = props => {
  return <Text>This is The Account Screen</Text>;
};

export default AccountScreen;
