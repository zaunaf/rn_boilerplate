import React, {useState} from 'react';
import {Text, View, Center} from 'native-base';

const HomeScreen = props => {
  return <Text>Hai. Welcome to The Home Screen</Text>;
};

export default HomeScreen;
